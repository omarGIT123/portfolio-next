/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add this line
  output: 'standalone',

  // You might have other settings here, like reactStrictMode
  reactStrictMode: true,
};

module.exports = nextConfig;
